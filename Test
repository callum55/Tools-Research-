# Task: Migrate "Concept-to-Code" from Claude Code to OpenAI Agents SDK + Temporal

## Paths (provided by user)
- NEW_PROJECT_PATH: <path to empty folder — build everything here>
- EXISTING_C2C_PATH: <path to existing Claude Code concept-to-code setup>
- ENVIRONMENTS_ROOT: <path to "Concept to Code Environments" folder containing concepttocode-{N}-* checkouts>

## Hard rules
1. EXISTING_C2C_PATH is READ-ONLY. Read it to extract prompts, skill rules,
   the art-direction library, and pipeline rules — never modify, move, or
   delete anything in it. All new code goes in NEW_PROJECT_PATH.
2. Replicate current behaviour exactly. Do not redesign the pipeline.
3. Python project. Dependencies: openai-agents==0.11.3, temporalio (latest),
   httpx, plus whatever you need for config/logging. Use a venv +
   pyproject.toml.
4. OS is Windows: use pathlib throughout, no hardcoded forward slashes,
   shell commands must work in PowerShell/cmd (or invoke git/dotnet/npm
   directly via subprocess without shell=True where possible).

## Step 1 — Discovery (do this first)
Read EXISTING_C2C_PATH and locate:
- The /concept-to-code slash command definition and orchestrator skill rules
  (SKILL.md). Check both the project folder AND ~/.claude (skills and
  subagent definitions may live under ~/.claude rather than the repo).
- Subagent definitions: requirements-analyst, art-director, data-architect,
  api-architect, frontend-architect, backend-dev, frontend-dev
- ~/.claude/skills/generate-image/generate-image.sh and the frontend-design
  art direction library
- Any pipeline rules docs (branch naming, reconciliation rules, test matrix)
Port each subagent's system prompt verbatim (adapt only tool references) into
agent instruction files under NEW_PROJECT_PATH/prompts/.

## Environment specifics
- Temporal: local dev server already running, connect to localhost:7233,
  default namespace. Do NOT reuse or modify the existing project at
  Documents\Projects\Temporal poc — that is a separate POC. New task queue
  name: "concept-to-code".

## Target architecture

### Proxy wiring (OpenAI Agents SDK) — two routes
The AI proxy exposes two OpenAI-compatible chat-completions routes
(no Responses API on either):
- openai route: 
  (models: gpt-5*, gpt-5.x-codex, gpt-image-2 etc.)
- vertex route: 
  (models: google/gemini-*, claude-* — note the google/ prefix on Gemini IDs)

Build a small client registry: one AsyncOpenAI per route (base URLs from env
vars OPENAI_ROUTE_URL / VERTEX_ROUTE_URL with the above defaults; same
OPENAI_API_KEY for both unless agentic_loop.py shows otherwise). Each agent
is constructed with
model=OpenAIChatCompletionsModel(model=<id>, openai_client=<route client>)
so agents can mix vendors. Call set_tracing_disabled(True) — there is no
OpenAI platform key and traces would 401. Force chat completions; do not use
the Responses API.

Verify early: BEFORE building the full pipeline, write and run a smoke test
that makes one chat completion WITH a tool call through EACH route. Tool/
function calling is a hard dependency — if either route strips or rejects
the tools parameter, stop and report which one.

						### Auth/TLS reference implementations (read-only)

 Replicate its auth header format exactly (default_headers on
AsyncOpenAI if it is not standard Bearer auth). If it hits a different base
path than the routes above, keep the base URLs above and take only the
header format.
TLS: internal endpoints may fail verification. Support SSL_CERT_FILE env var
for a corporate CA bundle; if unset and verification fails, allow
AsyncOpenAI(http_client=httpx.AsyncClient(verify=False)) behind an explicit
INSECURE_TLS=true env flag (local dev only, log a warning).
Do NOT replicate the delegated-auth/token-exchange machinery from
vault-ecosystem — this pipeline is single-user local; MCP servers use the
same commands/credentials as the existing Claude Code .mcp.json.
Truncate MCP tool results to ~8000 chars before returning them to agents
(mirrors _truncate_tool_result in the existing codebase).

### Model configuration
Single config file (config.py or yaml) mapping each agent to (route, model).
Two profiles, selectable via env var or CLI flag:

  profile=test   (cheap plumbing validation — expect rubbish code, that's fine)
    all agents: vertex / google/gemini-2.5-flash
    # fallback if tool-calling misbehaves on vertex: openai / gpt-5-nano

  profile=live
    requirements_analyst: vertex / google/gemini-2.5-flash
    art_director:         vertex / google/gemini-2.5-flash
    data_architect:       openai / gpt-5.4-mini
    api_architect:        openai / gpt-5.4-mini
    frontend_architect:   openai / gpt-5.4-mini
    backend_dev:          openai / gpt-5.2-codex
    frontend_dev:         openai / gpt-5.2-codex
    test_fix:             openai / gpt-5.2-codex

### Agents (OpenAI Agents SDK)
One Agent per current subagent, same names/roles as above. Shared tools
implemented as @function_tool:
- read_file, write_file, edit_file, list_dir — path-sandboxed to the active
  environment directory (reject any path outside it)
- run_bash — shell execution, cwd = environment dir (used for git, dotnet
  build, npm build, booting servers)
- generate_image — see below

### generate_image tool (native Python, replaces generate-image.sh)
Implement generate_image as a @function_tool that calls the proxy images API
directly (client.images.generate on the openai-route client, model
gpt-image-2) instead of shelling out to the bash script. Signature must
mirror the script:
- prompt (str, required)
- quality (str, default "medium" — the pipeline forces medium)
- size (str: 1024x1024 | 1536x1024 | 1024x1536, default 1024x1024)
- output_path (str) — write the returned .png (b64) to this path, sandboxed
  to the active environment's UI repo (public/images/)
- input_images (optional list of paths) — for edit/combine, matching the
  script's -i flag
Usage pattern to replicate: frontend_dev iterates the Visual Assets Plan
table in docs/frontend-architecture.md and generates every hero banner, spot
illustration, and avatar into public/images/.
Read ~/.claude/skills/generate-image/generate-image.sh for the exact API
request shape before implementing. Confirm /v1/images exists on the openai
route during the smoke test; if it 404s, stop and report back.

### MCP
Attach the currently configured MCP servers via the Agents SDK MCP support
(MCPServerStdio), reusing the exact commands from the existing Claude Code
.mcp.json:

<PASTE gitlab AND playwright ENTRIES FROM .mcp.json HERE>

Playwright MCP is required by the test_fix agent (browser_navigate,
browser_evaluate). gitlab MCP available to agents that currently use it.
Apply the ~8000-char tool-result truncation to all MCP results.

### Temporal
- Workflow: ConceptToCodeWorkflow(env_number: int, requirements_path: str)
- One worker process, one task queue ("concept-to-code"). All activities run
  on this worker (shared local filesystem is required).
- Every agent invocation = one activity. Agent activities: max_attempts=1
  (a half-completed dev agent must NOT be retried — repair happens via the
  workflow-level test-fix loop), heartbeats, start_to_close_timeout of
  30–60 min for dev/test activities.
- Workflow code is deterministic only: no I/O, no LLM calls, no filesystem
  access. All of that lives in activities.

### Phase mapping (must match current behaviour)
- Phase 0 (activity, no LLM): environment setup. Inputs: env number (1–8) +
  requirements path. git fetch / git reset --hard / git checkout -b on BOTH
  repos (API + UI) in ENVIRONMENTS_ROOT/concepttocode-{N}-*, shared branch
  feat/BSSBAT-<YYYYMMDDHHMMSS> (GitLab pre-receive hook requires
  <prefix>/<LETTERS>-<DIGITS>). Smoke-build both to validate templates.
- Phase A: requirements_analyst. In: raw requirements doc. Out:
  docs/normalized-spec.md, gaps filled autonomously with [ASSUMED] decisions.
- Phase B (parallel activities): art_director runs FIRST as a pre-step, then
  data_architect, api_architect, frontend_architect in parallel
  (asyncio.gather over activity handles in the workflow). Inputs:
  normalized-spec.md, Global Art Direction Ledger, art-direction-library.md.
  Outputs: docs/art-direction.md, docs/data-model.md, docs/api-contracts.md,
  docs/frontend-architecture.md. Then a reconciliation loop (workflow-driven,
  re-invoking architect agents) until all API permissions and routes match
  frontend expectations.
- Phase C+D (parallel activities): backend_dev + frontend_dev. Brief them to
  EXTEND the existing .NET 8 / Next.js scaffolds (e.g. add to Program.cs,
  DataSeeder.cs), never scaffold from scratch. Outputs: code diffs (models,
  controllers, EF InMemory stores, React components, Tailwind configs) and
  generated .png assets (heroes, avatars) in public/images/.
- Phase E (workflow loop, max 5 cycles): test_fix agent activity. Boots
  API/UI servers in background, runs validation matrix — Bash for builds,
  Playwright MCP for UI validation, HTTP fetch for API testing. Errors feed
  back into the next fix cycle. Exit early on green.
- Phase G (activity, no LLM): git add -A, git commit, git push origin
  <branch> on both repos. Never force-push. Do NOT create GitLab MRs
  (that stays manual).

### Entry point
CLI: python -m c2c.run <env-number> <path-to-requirements.md> [--profile test|live]
- starts the workflow on the local Temporal server (localhost:7233)
- separate command to run the worker: python -m c2c.worker
- README with setup steps (venv, temporal server start-dev, env vars,
  worker, run command, profile switching)

## Deliverables
Working project in NEW_PROJECT_PATH: agents, tools, MCP wiring, Temporal
workflow + activities + worker, ported prompts, config with both profiles,
README. Then do a dry-run explanation of one full pipeline execution tracing
every activity in order.

## Ask before assuming
If anything in EXISTING_C2C_PATH is ambiguous (e.g. reconciliation loop exit
criteria, smoke-build commands, Global Art Direction Ledger location), ask
the user rather than guessing.
