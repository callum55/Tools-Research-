Edit all 14 files in .opencode/agents/*.md. Add explicit
temperature, max_tokens, and reasoning effort to each YAML
frontmatter. Do not change model or permission blocks.

max_tokens are runaway-detectors, not targets — the incremental
write discipline (skeleton Write + one Edit per section) means
no healthy call should approach these:
- three architects, backend-dev, frontend-dev: temp 0.2, max_tokens 64000
- requirements-analyst: temp 0.3, max_tokens 64000
- backend-fixer, frontend-fixer: temp 0.2, max_tokens 16000
- api-tester: temp 0.1, max_tokens 8000
- ui-tester, visual-qa: keep variant: high, add max_tokens 8000
- art-director: temp 0.7, max_tokens 4000

Reasoning effort high for art-director, all three architects,
and both fixers. Use whatever key the vertex-claude provider
accepts for glm-5.2; if it rejects the key, report that rather
than guessing.

Print a table of every agent and its final settings.




The pipeline was written for a 1M-token context. It now runs on
256k in / 131k out. Large runs (30-40 features) risk crowding
the window, especially the long-lived orchestrator session.

1. Add a Phase A step: after writing traceability-manifest.md,
   count features. If >12, group them into bounded domains
   (e.g. Incidents, Risk Assessments, Training, Audits) and
   record the grouping in a new ## Domains section of the
   manifest.

2. When domains exist, api-architect writes
   docs/api-contracts-{domain}.md per domain plus a thin
   docs/api-contracts-index.md listing domains, their files,
   and shared conventions (versioning, ErrorEnvelope, auth,
   pagination). Same split for frontend-architecture.md.
   data-model.md stays single — it is the shared schema.

3. backend-dev and frontend-dev read the index plus only the
   domain files for the slice they are building, never all
   domain files at once.

4. Update reconciliation checks b-g and the Phase E testers to
   iterate over domain files via the index rather than assuming
   one file.

5. Add to SKILL.md orchestrator rules: after each Phase E cycle,
   write the consolidated failure list to docs/test-report.md
   and re-read from that file on later cycles rather than
   retaining full tester reports in session context.

Keep the single-file path working unchanged when features <=12.
Show me every diff and state how the ID scheme (API-F0N-0N)
stays stable across the split.
#









Implement a signature-moment round-trip modelled exactly on the
existing permission round-trip (Phase E Step 1.5), which works
because three independent sources must match and a grep proves it.

1. art-director output contract: add a SIGNATURE ID field
   (format SIG-01) alongside the existing SIGNATURE MOMENT prose.
   The moment must already name a component and file path — keep
   that requirement explicit.

2. frontend-architect: must list the SIG ID in §8 Component
   Inventory with the same component name and path.

3. frontend-dev: the component must render the root element with
   data-signature="SIG-01".

4. visual-qa: new HARD FAIL check — assert the attribute exists
   in the rendered DOM at the declared route, and screenshot it.
   Not eligible for STRUCTURED DOWNGRADE.

5. New reconciliation check g in Phase B Step 5: grep the SIG ID
   in art-direction.md and frontend-architecture.md §8. Mismatch
   or absence re-spawns frontend-architect.

Edit SKILL.md and the relevant agent files. Show me every diff.







art-direction.md declares an ILLUSTRATION STYLE TOKEN that can
ban raster on data surfaces. Nothing enforces it — a prior run
shipped a stock clipart banner on a table page.

Write scripts/validate-imagery.mjs:
- Parse art-direction.md for whether raster is permitted, and on
  which surfaces.
- List every file in <ui-folder>/public/images/ (excluding
  avatars/, which are explicitly permitted).
- Cross-reference frontend-architecture.md §10b Visual Assets
  Plan: every image file must appear there with a purpose and an
  art-direction token.
- Exit non-zero with a per-file reason on any unlisted image, or
  any raster where the token bans it.

Wire it as a blocking gate at Phase C+D exit, before Phase E.
Print the script and the SKILL.md change.



Two config changes, then verification. Report facts; do not
guess where you cannot observe.

CHANGE 1 — reasoning variant
Confirm from ~/.config/opencode/opencode.json that a "max"
variant exists for BOTH vertex-claude/glm-5.2 and
azure-openai/gpt-5.6-*. Then set variant: max on every agent
currently on variant: high. Leave temperature and maxTokens
unchanged. If either provider has no max variant, leave those
agents on high and say which.

CHANGE 2 — visual-qa model tier
Confirm azure-openai/gpt-5.6-terra is available on the
azure-openai provider. If yes, change visual-qa only from
gpt-5.6-luna to gpt-5.6-terra, keeping its other settings.
Leave ui-tester and playwright-probe on luna. If terra is not
available, change nothing and say so.

VERIFY
a) Check for a duplicate/competing reasoning key: some agents
   may have both variant: max and a separate reasoning-effort
   field in frontmatter. Report any agent with both.
b) Run one trivial task through a glm-5.2 agent with
   variant: max and capture the outbound request opencode
   sends (verbose/debug logging or proxy access log). Confirm
   whether reasoningEffort: "max" is actually present in the
   payload. If you cannot observe the payload, say so plainly
   rather than inferring from config.
c) Search opencode.json, the vertex-claude proxy config, and
   all agent files for ANY timeout setting. Report the exact
   values found, or state that none exist.
d) Note wall-clock duration of the test task in (b).

Print the final 16-agent table: agent, model, temperature,
maxTokens, variant.




Three fixes:

1. Five agents show "–" in the variant column: api-tester,
   backend-dev, build-verifier, frontend-dev,
   requirements-analyst. Set variant: max on backend-dev,
   frontend-dev, and requirements-analyst. Leave api-tester and
   build-verifier without a variant (short deterministic
   outputs, no benefit).

2. The earlier run reported 16 agents; this table shows 14.
   List every file in .opencode/agents/ and confirm none were
   lost or overwritten. Report the true count.

3. instructions.md line 35 documents a ~5-min SSE idle timeout
   on the GCP load balancer. Quote that line verbatim and state
   whether it describes idle time between streamed tokens or
   total request duration. Then check whether opencode retries
   or fails on SSE disconnect, and what the pipeline's Subagent
   Reliability Protocol does with that failure.

Print the corrected table with a row per agent file found.



Test whether variant: max trips the 5-min SSE idle timeout,
without running the pipeline.

Run frontend-architect (glm-5.2, variant: max, maxTokens 64000)
as a one-shot with a prompt heavy enough to force deep reasoning
before any output — e.g. "Design the complete component
hierarchy, route map, state management strategy and data
fetching layer for a 40-feature health-and-safety management
SPA. Think it through fully before writing anything, then output
the full architecture."

Capture and report:
- elapsed seconds from request to FIRST token
- total wall-clock
- whether it completed or errored
- the error text if any
- reasoning token count

Repeat once with the same agent on variant: high for comparison.
Report both.




Implement a signature-moment round-trip modelled exactly on the
existing permission round-trip (Phase E Step 1.5), which works
because three independent sources must match and a grep proves it.

1. art-director output contract: add a SIGNATURE ID field
   (format SIG-01) alongside the existing SIGNATURE MOMENT prose.
   The prose must already name a component and file path — keep
   that requirement explicit.

2. frontend-architect: must list the SIG ID in §8 Component
   Inventory with the same component name and path. Under the
   domain split, §8 lives in whichever file owns that component —
   the index must record which.

3. frontend-dev: the component must render its root element with
   data-signature="SIG-01".

4. visual-qa: new HARD FAIL check — assert the attribute exists
   in the rendered DOM at the declared route, and screenshot it.
   Not eligible for STRUCTURED DOWNGRADE.

5. New reconciliation check g in Phase B Step 5: grep the SIG ID
   in art-direction.md and in frontend-architecture (index or
   domain file). Mismatch or absence re-spawns frontend-architect.

Update SKILL.md, the agent files, and TECH_DOSSIER.md §4.4 and
§5.1. Show me every diff.




art-direction.md declares an ILLUSTRATION STYLE TOKEN that can
ban raster on specific surfaces. Nothing enforces it — a prior
run (env 4, Editorial Precision, medium = editorial line-art /
fine engraving, "never raster") shipped a full-bleed stock
clipart banner on a data table page.

Write scripts/validate-imagery.mjs:
- Parse art-direction.md for the ILLUSTRATION STYLE TOKEN and
  IMAGERY INTENSITY: whether raster is permitted, and on which
  surfaces it is banned.
- List every file in <ui-folder>/public/images/, excluding
  avatars/ (per-user photoreal avatars are explicitly permitted
  by the contract).
- Cross-reference frontend-architecture §10b Visual Assets Plan:
  every image file must appear there with a filename, purpose,
  art-direction token, dimensions, and alt text. Under the domain
  split, resolve §10b via the index.
- Exit non-zero with a per-file reason for: any image not listed
  in §10b, or any raster file where the token bans raster.

Wire it as a blocking gate at Phase C+D exit, before Phase E —
same pattern as the traceability validator at Phase A exit.
Update SKILL.md, TECH_DOSSIER.md §5.1 gate inventory, and add
it to §6.3 validation coverage.

Print the script and every diff.
