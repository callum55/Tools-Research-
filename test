Three items, in order.

1. Extend the domain floor. Currently only VISUAL_DENSITY is clamped
   for B2B/admin/finance. DESIGN_VARIANCE and MOTION_INTENSITY_DIAL
   are freely chosen, and their gates (>= 6 and >= 4) can be dodged
   by declaring 5 and 3. Add floors so those gates cannot be
   switched off by the agent they constrain. Propose the values
   before writing them.

2. Restore the always-required assets in validate-imagery.mjs:
   1 login/hero, 1 empty-state, 1 error-page illustration, required
   regardless of declared IMAGERY INTENSITY. As written, declaring
   "restrained-but-present" exempts a project from the floor
   entirely. Keep the 3/5/8 tiering above it.

3. Then run the round-trip. Add DESIGN_VARIANCE: 8,
   MOTION_INTENSITY_DIAL: 5, VISUAL_DENSITY: 8 to one existing
   art-direction.md by hand. Run the pipeline. Report:
     - does frontend-architect carry all three into §10 verbatim
     - does frontend-dev render data-density / data-variance on root
     - can visual-qa read them back from the DOM
     - does reconciliation check f actually fail if a dial is absent
   Test the failure path, not just the happy path.

Show all diffs. Commit only after the round-trip passes.
