Repo: claude-concept-to-code. READ-ONLY. Produce a proposal.
Change no files. I approve before any edit.

CONTEXT
Generated UIs converge on a sterile look: grey-on-white, hairline
borders, no illustration, no KPI band, no charts, flat nav. Two
runs, different requirements docs, near-identical output. All
gates passed.

Every gate we have is SUBTRACTIVE. We have a filter and no floor.

Three independent projects converged on the same three dials:
 - github.com/nextlevelbuilder/ui-ux-pro-max-skill (variance /
   motion / density, 1-10, offline Python search over 192 palettes,
   84 styles, 74 font pairings; supports OpenCode)
 - github.com/Leonxlnx/taste-skill (DESIGN_VARIANCE /
   MOTION_INTENSITY / VISUAL_DENSITY, 1-10)
 - github.com/miqdadbadjuber/anti-slop (ENERGY / RHYTHM / MOTION, 1-3)
Our anti-repeat gate measures HSL hue. None of them measure hue.

Anthropic's frontend-design skill names three looks AI defaults to.
Two match our library worlds: near-black + single bright accent
(= Technical Blueprint), and broadsheet with hairline rules and
zero radius (= Editorial Precision).

PART 1 - AUDIT OURS
1.1 For each gate (validate-imagery, validate-coverage, anti-repeat,
    reconciliation a-m, visual-qa checklist, design-critic rubric):
    classify every check SUBTRACTIVE or ADDITIVE. Give the ratio.
1.2 Trace the logic and answer: can a UI with zero illustrations,
    zero charts, no KPI band and flat nav pass every current gate?
    If yes, quote the gates it passes.
1.3 List every world in art-direction-library.md with its own
    description. Mark any matching the three named AI defaults.
1.4 Do we record density, layout variance, or motion intensity
    ANYWHERE as a measured value? Quote it or state absent.

PART 2 - EVALUATE THEIRS (fetch the actual repos)
2.1 ui-ux-pro-max: verify offline operation (no network calls),
    Python version needed, and what --design-system --persist
    actually emits. Compare its MASTER.md shape against our
    art-direction.md schema. Could it replace or feed our library?
    State integration cost honestly.
2.2 taste-skill: extract its dashboard-hardening and anti-center-bias
    rules verbatim. Are they expressible as deterministic checks
    against our rendered DOM?
2.3 For each of the three, list what is landing-page-specific and
    therefore noise for an internal B2B console. Be blunt.

PART 3 - PROPOSE
3.1 Three-dial schema for our art-direction output: field names,
    ranges, where declared, which agent verifies each, and the
    exact check. Follow the SIG-01 round-trip pattern.
3.2 Positive floors for validate-imagery.mjs: minimum asset count
    and mandated placement. Show the exit-gate logic.
3.3 The 2-3 conditional structural rules worth transplanting,
    rewritten for a data-dense admin console at DENSITY 8.
3.4 Verdict on ui-ux-pro-max: adopt wholesale / harvest data only /
    ignore. Commit to one, with reasoning and cost.
3.5 What to do with Editorial Precision and Technical Blueprint.

PART 4 - SEQUENCE
Order the changes by (impact / effort), state dependencies, and
name the ONE change to make first. For each, give the cheapest
experiment that proves it worked.

Cite file:line for ours, URL for theirs. Mark anything unverified.
Do not propose fixes you cannot ground in evidence.
