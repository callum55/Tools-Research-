Task: parallelise Phase E. Two stages. Stop and report after stage 1.

STAGE 1 - concurrency safety at width 2
Do not modify any agent file yet.
Spawn design-critic and ui-tester SIMULTANEOUSLY against the env 7
build (both servers up first). design-critic: the same 4 routes.
ui-tester: its normal route set.

Report:
 - did both spawn, both complete, both write report files
 - any Playwright timeout, browser-launch failure, or MCP error
   (verbatim)
 - whether the two agents got separate browser contexts or contended
   for one - cite evidence (process list, screenshot filenames,
   /tmp/opencode collisions)
 - wall-clock for each, and total elapsed for the pair
 - design-critic scores: do they still match the 3.2 baseline?

If either agent fails or the scores move, STOP and report. Do not
proceed to stage 2.

STAGE 2 - only if stage 1 was clean
Modify the orchestrator so Phase E fans out:
 - ui-tester, visual-qa and design-critic spawn concurrently, not
   in sequence
 - Phase E completes when all three have written report files
 - if any produces no report file, Phase E FAILS and the run stops -
   no handover, no Phase F

Also add per-phase timing to a run log on disk: phase, start, end,
duration, agents spawned, agent start/stop times. Write it as the run
progresses, not at the end, so a killed run still leaves evidence.

Report the diff and the new Phase E structure. Do not run a full
pipeline yet.
