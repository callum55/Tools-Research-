Agents are no longer loadable in opencode. Diagnose without
changing anything yet.

1. Run: opencode debug config — report any parse or validation
   errors.
2. Run: opencode debug agent <name> for api-architect,
   frontend-dev, and visual-qa. Report output or error for each.
3. Print the raw YAML frontmatter (from opening --- to closing
   ---) of all 14 .opencode/agents/*.md files, verbatim.
4. Check each for: missing or duplicated --- delimiters, keys
   added inside the markdown body instead of frontmatter,
   inconsistent key naming (maxTokens vs max_tokens vs
   max_tokens), tabs instead of spaces, and any key opencode
   does not recognise.
5. Check the opencode log at ~/.local/share/opencode/log/
   for load errors since the edits started today.

Report findings. Do not edit yet.
