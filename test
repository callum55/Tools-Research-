Single task. Do not run Phase E. Do not spawn ui-tester or visual-qa.
Change no files except the report design-critic writes.

Spawn ONE agent: design-critic (.opencode/agents/design-critic.md)
against the existing env 5 FinFlow build.

Before spawning, state the model, variant, temperature, top_p and
maxTokens as they will actually be sent.

Routes to score: / (dashboard), /escalations, /vena, /login

Report:
 - did it spawn, complete, and write its report file
 - scores per route per category, verbatim
 - its written justification, verbatim
 - screenshots captured vs screenshots actually sent to the model

Then answer directly:
 1. Did it flag the dashboard subtitle rendering
    "Your operations overview for ." with an empty value?
 2. Did it flag "1 Jan 1970" in the /vena Implemented column?
 3. Did it flag that the dashboard's only content is an empty state?
 4. Did it flag the absence of any illustration, chart or KPI band?
 5. Under the current rubric (3 = failing, fix triggers below 4),
    does this build pass or fail?

If it hangs or does not return within 10 minutes: kill it, and report
exactly where it stopped and what its last tool call was.
