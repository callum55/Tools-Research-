Implement step 1 only: record the three dials in art-direction.md.

DESIGN_VARIANCE / MOTION_INTENSITY / VISUAL_DENSITY, integers 1-10,
in the Output Contract at art-direction-library.md:51-86, emitted by
art-director.md, verified present by reconciliation check f.

ADDITION not in the proposal: the art-director must NOT freely choose
these. Add a domain floor to art-director.md — for B2B / admin /
finance / operations domains, VISUAL_DENSITY must be >= 7 and a lower
value is a BLOCKER. State how you are implementing the floor before
you write it.

Separately, in design-critic.md, change the fix-cycle trigger from
score <= 2 to score < 4, and reword the rubric so that 3 reads as a
FAILING grade rather than "adequate". Show me the current wording
before you change it.

Then: add the three dials to one existing art-direction.md by hand,
run the pipeline, and report whether frontend-architect carries them
into §10 and whether visual-qa can read them back.

Show diffs. Do not commit.
