Read-only forensic review of the last end-to-end run.
Change no files. Do not re-run any phase. Do not spawn any agent.
Every answer must cite a file path, file:line, or log excerpt.
If something cannot be evidenced from disk, say "no evidence on disk"
rather than inferring it.

1. RUN SHAPE
   - Which env, which requirements doc, run start and end timestamps.
   - Per phase (A-E): completed / failed / killed, wall-clock duration,
     and the agents actually spawned in each.
   - For any agent that did not complete: its last tool call, last log
     line, and the elapsed time before it stopped.

2. MODEL CONFIG AS SENT
   For every agent that ran, state model, reasoning variant,
   temperature, top_p, maxTokens as actually sent (not as documented).
   Flag any mismatch against the agent's .opencode/agents/*.md file.

3. LUNA AGENTS
   - Did ui-tester, visual-qa, playwright-probe, design-critic each
     spawn? Complete? Write their report files? Give paths.
   - For visual-qa: screenshots captured vs screenshots actually sent
     to the model, per batch. Any HTTP 400s or timeouts, verbatim.

4. DESIGN-CRITIC
   - Report file path. Scores per route per category, verbatim.
   - Its written justification, verbatim.
   - Under the current rubric (fix triggers below 4), pass or fail?
   - Did the fix cycle trigger? How many cycles, and what changed?

5. GATES FROM 398ba85
   For each gate, state: did it execute, what did it return, evidence.
   - VISUAL_DENSITY >= 7 domain floor
   - DESIGN_VARIANCE and MOTION_INTENSITY (floors absent - what values
     were declared, and were they checked at all?)
   - imagery minimums
   - KPI band and tabular-nums checks
   - signature-moment round-trip: SIG-01 in art-direction ->
     listed in §8 with route -> data-signature present in built DOM.
     Grep the built output, not the docs.
   If a gate lives inside an agent that did not complete, say so
   explicitly and mark it INERT.

6. ART DIRECTION
   - World selected, and the last 3 ledger entries.
   - Anti-repeat gate: what distance did it compute, against what,
     and did it pass?
   - The three dial values declared in art-direction.md §10.
   - Reference layout for the selected world vs what was built:
     list each element the reference specifies and whether it exists
     in the DOM.

7. VALIDATORS
   Run validate-imagery.mjs --self-test, validate-coverage.mjs and
   validate-traceability.mjs against this build. Exit codes and
   output verbatim. Note known false positives rather than fixing.

8. DRIFT
   - Diff .claude/agents/ against .opencode/agents/: which files
     differ, and did the run read from the stale copies?

Finish with a numbered list of every failure found, ordered by
whether it is infrastructure (agent didn't run) or design (agent ran
and produced bad output). Do not propose fixes.
